const isRaining = true;
const person = {
    age: 27
}

// valid - we just changing one of the objects properties
person.age = 28;

// invalid
//person = {};

// isRaining = false;

console.log(isRaining);
console.log(person);
